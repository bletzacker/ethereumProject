{
  "ignore": [
    "README.md",
    ".github",
    "LICENSE"
  ],
  "commands": {
    "Compile": "truffle compile",
    "Migrate": "truffle migrate",
    "Test contracts": "truffle test",
    "Test dapp": "cd app && npm test",
    "Run dev server": "cd app && npm run start"
  },
  "hooks": {
    "post-unpack": "npm install && cd app && npm install"
  }
}
